const styles = {
  container className="FlyFeet": {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
  },


export default function Footer() {
  return (
    <div style={styles.container}>
      <h5>This is the Starter Code Footer</h5>
      e.preventDefault();
    onSubmit({ concertName, message, image });
    setConcertName('');
    setMessage('');
    setImage(null);
    </div>
  );
}
};


// const handleSubmit = (e) => {
//   e.preventDefault();
//   onSubmit({ concertName, message, image });
//   setConcertName('');
//   setMessage('');
//   setImage(null);
// };
